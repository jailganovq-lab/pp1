topics: while;  for


1) If this number is a palindrome, output YES; else NO.
code:
#include <iostream>
using namespace std;
int main() {
    int num;
    cin >> num;
    int org = num;
    int r = 0;
    while (num > 0) {
        int digit = num % 10;
        r = r * 10 + digit;
        num /= 10;
    }
    if (org == r)
        cout <<"yes" << endl;
    else
        cout << "no" << endl;
    return 0;
}

2) In this task, you should find the sum of all numbers from 1 to n.
code:
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    long long sum = 0;  
    for (int i = 1; i <= n; i++) {
        sum += i;  
    }
    cout << sum << endl;
    return 0;
}


3) Given a sequence consisting of n integers, write a program that finds the sum of all elements.
code:
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    long long sum = 0;  
    for (int i = 0; i<n; i++) {
        int d;
        cin >> d;
        sum+=d;
    }
    cout << sum << endl;
    return 0;
}



4) Given a sequence consisting of n integers, write a program that outputs the count of the positive numbers in this sequence.
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    long long sum=0;  
    for (int i = 0; i<n; i++) {
        int d;
        cin >> d;
        if (d>0){
            int x=0;
            x+=1;
            sum+=x;
        }        
    }
    cout << sum << endl;
    return 0;
}


5) Given an integer n, print YES if it is a power of two, otherwise, print NO. Note, an integer  is a power of two, if there exists an integer  such that 
code:
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    int p = 1;  
    while (p < n) {
        p = p * 2;  
    }
    if (p == n)
        cout << "YES";
    else
        cout << "NO";

    return 0;
}


6) разница между мах и мин
code:
#include <iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    int x;
    cin >> x;
    int maxN=x;
    for(int i=1; i<n; ++i){
        cin >>x;
        if(x>maxN){
            maxN = x;
        }
    }
    cout << maxN << endl;
}


7) Single integer — the position of the maximum in the sequence.
code:
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    int num;
    int maximum = -1000000000; 
    int pos = 1;               
    for (int i = 1; i <= n; i++) {
        cin >> num;
        if (num > maximum) {
            maximum = num;
            pos = i; 
        }
    }
    cout << pos;

    return 0;
}

8)pow of two
code:
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    int p = 1;  
    while (p <= n) {
        cout << p << " ";
        p = p * 2;  
    }    
    return 0;
}


9)Fibanaci number
code:
#include <iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    int a = 0, b = 1;
    for(int i=0; i<n; ++i){
        cout << a << " ";
        int c = a+b;
        a=b;
        b=c;
    }
    cout << endl;
}
